query PendingDirectDeposits {
    directDeposits(orderBy: bnInit, where: {pending: true}) {
        id
        zkAddress_pk
        zkAddress_diversifier
        deposit
    	fallbackUser
        tsInit
        txInit
    }
}